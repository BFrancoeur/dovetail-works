# Postgres
POSTGRES_USER=payload
POSTGRES_PASSWORD=changeme
POSTGRES_DB=payload_db

# Payload CMS
PAYLOAD_SECRET=super-secret-change-this-min-32-chars

# Next.js
NEXT_PUBLIC_SERVER_URL=https://localhost

# Anthropic (Claude API — for AI report generation)
ANTHROPIC_API_KEY=sk-ant-...

# Email (Mailpit in dev)
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_FROM=noreply@localhost
